<h1>Done tasks</h1>
<% if @tasks.size == 0 %>
<p>No tasks done.</p>
<% end %>
<table>
  <tr>
    <th>Task</th>
    <th>Due date</th>
    <th></th>
   </tr>
  	<%= render @tasks %>
</table>
<%= will_paginate(@tasks) %>
<%= link_to 'Back', :action => :index %>
